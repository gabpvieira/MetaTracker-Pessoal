import React from 'react'
import ReactDOM from 'react-dom/client'
import MetaTrackerApp from './meta_tracker_app.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MetaTrackerApp />
  </React.StrictMode>,
)